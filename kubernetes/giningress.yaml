apiVersion: kubernetes.io/ingress.class
kind: Ingress
metadata:
  name: gin-ingress
  namespace: doha-namespace
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: gindatetimeserver.com
      http:
        paths:
          - path: /datetime
            pathType: Prefix
            backend:
              service:
                name: app-service
                port:
                  number: 8080