apiVersion: v1
kind: Service
metadata:
  name: gin-datetime-service
  namespace: doha-namespace
spec:
  selector:
    app: gin-datetime
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080