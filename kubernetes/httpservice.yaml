apiVersion: v1
kind: Service
metadata:
  name: http-datetime-service
  namespace: doha-namespace
spec:
  selector:
    app: http-datetime
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090